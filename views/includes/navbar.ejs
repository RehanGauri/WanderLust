<style>
  /* === DESKTOP STYLES (UNCHANGED) === */
  .search-btn {
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0 1.5rem;
    height: 2.4rem;
  }
  .search-btn:hover {
    background-color: #e63946;
    color: #fff;
  }
  .search-btn i {
    margin-right: 0.5rem;
  }
  .search-input {
    border-radius: 25px;
    padding: 0.5rem 1.5rem;
    width: 15rem;
    max-width: 100%;
    border: 1px solid #ccc;
    height: 2.4rem;
  }
  .nav-link.btn-link {
    border: 1px solid #ccc;
    border-radius: 25px;
    padding: 0.4rem 1.2rem;
    transition: all 0.3s ease;
    color: #333 !important;
    font-size: 0.9rem;
  }
  .nav-link.btn-link:hover {
    background-color: #fe424d;
    color: #fff !important;
    border-color: #fe424d;
  }
  .btn-signup {
    background-color: white;
    color: #fe424d !important;
    border: 1px solid #fe424d;
    border-radius: 25px;
    padding: 0.4rem 1.2rem;
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }
  .btn-signup:hover {
    background-color: #fe424d;
    color: #fff !important;
  }
  .btn-login {
    background-color: #fe424d;
    color: #fff !important;
    border-radius: 25px;
    padding: 0.4rem 1.2rem;
    font-size: 0.9rem;
    border: none;
    transition: all 0.3s ease;
  }
  .btn-login:hover {
    background-color: #e63946;
    color: #fff !important;
  }
  .navbar-brand i {
    color: #fe424d;
    font-size: 1.7rem;
  }
  .navbar-nav form {
    margin: 0;
  }
@media (max-width: 992px) {
  .navbar {
    padding-top: 1rem;
    padding-bottom: 1.2rem;
    height: 7rem;
  }

  .btn-login.btn-sm,
  .btn-signup.btn-sm {
    font-size: 0.8rem;
    padding: 0.50rem 1rem !important;
  }
  .btn-login.btn-sm{
    padding: 0.55rem 1.1rem !important;
  }
  .nav-link.btn-link {
    font-size: 0.85rem;
    padding: 0.3rem 1rem;
  }

  .navbar-brand i {
    font-size: 1.5rem;
  }
  .search-input{
    height: 35px;
  }
  .search-btn{
    height: 35px;
  }
}
@media (max-width: 420px) {
  .navbar{
    padding-left: 0.5rem !important;
    padding-right: 0.5rem !important;
  }
  .search-btn{
    padding: 0 1rem;
  }
}
@media (max-width: 369px) {
.search-btn{
    padding: 0 0.5rem !important;
  }
    .search-input{
      width: 10rem !important;
  }
}

</style>

<!-- Mobile Structure -->
<!-- ✅ MOBILE NAVBAR -->
<nav class="navbar navbar-light bg-light border-bottom sticky-top px-4 py-3 d-flex d-lg-none">
  <div class="d-flex flex-column w-100 gap-2">

    <!-- Top Row: Left Group + Auth -->
    <div class="d-flex justify-content-between align-items-center w-100">

      <!-- Left Group: Logo + Explore + Add New -->
      <div class="d-flex align-items-center gap-1">
        <a class="navbar-brand me-2" href="/listings"><i class="fa-solid fa-compass"></i></a>
        <a class="nav-link btn-link py-1 px-3" href="/listings/new">Add New</a>
      </div>

      <!-- Auth Buttons (smaller) -->
      <div class="d-flex align-items-center gap-2">
        <% if (!currUser) { %>
          <a class="btn-signup btn btn-sm px-3 py-1" href="/signup">Sign up</a>
          <a class="btn-login btn btn-sm px-3 py-1" href="/login">Log in</a>
        <% } %>
        <% if (currUser) { %>
          <a class="btn-login btn btn-sm px-3 py-1" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>

    <!-- Search Bar -->
    <!-- MOBILE NAVBAR FORM -->
<form class="d-flex align-items-center w-100 gap-2 mt-2 search-form" role="search" action="/listings" method="GET">
  <input name="search" value="<%= typeof search !== 'undefined' ? search : '' %>" class="form-control search-input flex-grow-1" type="search" placeholder="Search destinations">
  <button class="btn search-btn" type="submit">
    <i class="fa-solid fa-magnifying-glass"></i>Search
  </button>
</form>

    
  </div>
</nav>



<!-- Desktop Structure (unchanged) -->
<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom sticky-top px-4 py-2 d-none d-lg-flex">
  <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass"></i></a>

  <div class="collapse navbar-collapse justify-content-between align-items-center">
    <!-- Left nav links -->
    <div class="navbar-nav d-flex align-items-center gap-2">
      <a class="nav-link btn-link" href="/listings">Explore</a>
      <a class="nav-link btn-link" href="/listings/new">Add New Listing</a>
    </div>

    <!-- Centered search bar -->
    <div class="navbar-nav">
      <!-- DESKTOP NAVBAR FORM -->
<form class="d-flex align-items-center search-form" role="search" action="/listings" method="GET">
  <input name="search" value="<%= typeof search !== 'undefined' ? search : '' %>" class="form-control me-2 search-input" type="search" placeholder="Search destinations">
  <button class="btn search-btn" type="submit">
    <i class="fa-solid fa-magnifying-glass"></i>Search
  </button>
</form>

    </div>

    <!-- Auth buttons -->
    <div class="navbar-nav d-flex align-items-center auth-buttons gap-2">
      <% if (!currUser) { %>
        <a class="btn-signup btn" href="/signup">Sign up</a>
        <a class="btn-login btn" href="/login">Log in</a>
      <% } %>
      <% if (currUser) { %>
        <a class="btn-login btn" href="/logout">Log out</a>
      <% } %>
    </div>
  </div>
</nav>